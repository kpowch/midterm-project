<%# Flash partial for all flash messages %>
<% if (errors.length) { %>
  <% errors.forEach((error) => { %>
    <p>Error: <%= error %></p>
  <% }) %>
<% } %>
<% if (info.length) { %>
  <% info.forEach((message) => { %>
    <p>Info: <%= message %></p>
  <% }) %>
<% } %>
